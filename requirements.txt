# Gold_FX Trading System - Phase 2 Requirements
# ==============================================

# Core Dependencies (Required)
pandas>=1.5.0
numpy>=1.21.0
pyyaml>=6.0
python-dateutil>=2.8.0
pytz>=2022.1

# MT5 Integration (Required for live trading)
MetaTrader5>=5.0.45

# Database (Required)
sqlite3  # Built-in with Python

# Logging and Utilities (Required)
colorlog>=6.7.0

# Machine Learning Dependencies (Optional - with fallbacks)
# Install these for full ML strategy functionality:
tensorflow>=2.10.0,<3.0.0  # For Ensemble NN and RL Agent
xgboost>=1.6.0              # For XGBoost Classifier
scikit-learn>=1.1.0         # For feature engineering and metrics

# Additional ML utilities (Optional)
joblib>=1.2.0               # For model serialization

# Development and Testing (Optional)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0

# Performance monitoring (Optional)
psutil>=5.9.0

# Note: The system is designed to work without ML dependencies
# If TensorFlow, XGBoost, or scikit-learn are not available,
# the strategies will automatically fall back to simulation mode
# with heuristic-based signal generation.

# Installation Instructions:
# ========================
# 
# Minimal installation (Core functionality only):
# pip install pandas numpy pyyaml python-dateutil pytz MetaTrader5 colorlog
#
# Full installation (All ML capabilities):
# pip install -r requirements.txt
#
# Development installation:
# pip install -r requirements.txt pytest pytest-cov black flake8